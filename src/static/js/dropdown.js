const DropDownBtns = document.querySelectorAll('.dropdown');
const DropDowns = document.querySelectorAll('.left-dropdown-menu');

DropDownBtns.forEach(btn => {
    btn.addEventListener('click', () => {
        //pass
    });
});